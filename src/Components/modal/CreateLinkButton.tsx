import React from 'react';
import "../../styles/CreateLinkButton.css"

const CreateLinkButton = () => {
    return (
        <button className="CreateLinkButton">
            Создать ссылку
        </button>
    );
};

export default CreateLinkButton;